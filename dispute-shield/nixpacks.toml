[phases.setup]
nixPkgs = ["php83", "php83Extensions.mongodb", "phpPackages.composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader --no-interaction --ignore-platform-req=ext-mongodb"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t . router.php"
